import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const InboxScreen = () => {
	return (
		<View>
			<Text>navigation stacks</Text>
		</View>
	);
};
export default InboxScreen;

const styles = StyleSheet.create({});
